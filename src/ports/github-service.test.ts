import { describe, expect, it } from "vitest";

// This file defines interfaces only; minimal runtime smoke to satisfy guardrails.
describe("github-service types", () => {
	it("has no runtime exports to break", () => {
		expect(true).toBe(true);
	});
});
